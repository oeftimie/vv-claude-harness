# Claude Progress Log
# Project: vv-claude-harness
# Created: 2026-07-22

## Session 1 - Initialization (OVI-44 step 0, dogfood bootstrap)
- Verified baseline: test/run-tests.sh 66/66 passing on main @ d3661ff
- Created harness scaffolding (.harness/: harness.json, features.json, init.sh, context_summary.md, claude-progress.txt)
- Stack: custom (full_test = bash test/run-tests.sh; smoke_test = bash -n hooks/*.sh + json.tool on .claude-plugin/*.json)
- Git identity: Ovidiu Eftimie <eovidiu@gmail.com>, gh account eovidiu over HTTPS (SSH unavailable in this environment)
- Imported OVI-44's 21 sub-issues as features F001-F021 (depends_on mirrors the epic graph)
- Installed quality-gate hooks (.claude/hooks/) and .claude/settings.json
- Next: per-issue loop starting with F001 / OVI-47
- 2026-07-22 /harness-issue-prep OVI-47 -> F001: SV ASK (2 Qs answered), RV PASS; normalized spec + fresh spec.hash written to features.json; local-only (no stamp/label, Linear untouched)

## Session 2 - 2026-07-22 (single-session)
- Feature: F001 - [OVI-47] P0.2 single-owner truth fixes
- Status: complete (PR #23 merged to main @ d5c0e0f; OVI-47 Done in Linear)
- Tests: 76/76 passing (66 baseline + 10 new; TDD red phase confirmed 9 failing first)
- Coverage: n/a (shell suite, no coverage tooling; full_test is the gate)
- Decisions: fixture frozen at 4.0.0 with _note; MIT license; README v2.x dates live under "The evolution" heading (see context_summary.md)
- Next: fresh session — /harness-issue-prep on the next P0 issue (F002), then /harness-continue; bootstrap-era spec hashes on F002-F021 are cosmetic, prep each before implementing
- Blockers: none (gh pr merge is permitted via .claude/settings.json; Claude completes the loop through merge)
- 2026-07-22 /harness-issue-prep OVI-48 -> F003: SV ASK (4 Qs answered), RV PASS (cycle 1); normalized spec + fresh spec.hash written to features.json; local-only (no stamp/label, Linear untouched)

## Session 3 - 2026-07-22 (single-session)
- Feature: F003 - [OVI-48] P0.3 hook template robustness + fixture test coverage
- Status: complete; PR #25 formally approved by both reviewers (reviewer: REQUEST CHANGES -> blocker + nit fixed in a4a7119 with red-first regression tests -> re-verdict APPROVE, repro independently re-run; reviewer2: nit withdrawn on evidence -> clean APPROVE); merged by Claude @ 7f0ee74 (squash; note: repo convention is merge commits — use --merge next time); OVI-48 Done in Linear
- Tests: 115/115 passing (77 baseline + 38 new across TDD and review cycles; red phases confirmed 13, then 4 failing first)
- Coverage: n/a (shell suite, no coverage tooling; full_test is the gate)
- Decisions: CLAUDE_PROJECT_DIR anchor in all 4 gate templates; verify-task-quality is the sole non-lead features.json writer (targeted, atomic, trailing newline) — details in context_summary.md
- Next: /harness-issue-prep OVI-46 -> F002 (next P0, still unprepped; do NOT trust its bootstrap spec.hash), then implement. Also refresh this repo's live .claude/hooks/*.sh from the fixed templates (out of F003 scope, deferred deliberately)
- Blockers: none (gh pr merge allowed; the auto-mode classifier may block a first attempt — retry after confirming with Ovidiu)
- 2026-07-23 /harness-issue-prep OVI-46 -> F002: SV PASS direct (0 questions, no RV loop needed; SV ran twice due to delayed delivery, both PASS, retry confirmed no drift); normalized spec + fresh spec.hash written to features.json; local-only (no stamp/label, Linear untouched)

## Session 4 - 2026-07-23 (single-session)
- Feature: F002 - [OVI-46] P0.1 arm scope enforcement in the operational spawn path
- Status: implementation complete; PR pending, CI + adversarial review + merge in progress this session
- Tests: 124/124 passing (115 baseline + 9 new; TDD red phase confirmed 2 failing first — the other 3 new cases were vacuously green pre-implementation since nothing warned yet)
- Coverage: n/a (shell suite, no coverage tooling; full_test is the gate)
- Decisions: warning keyed on (status in-progress AND assigned_to set) AND hook installed AND scope file missing; same try/except-pass + `|| true` guard pattern as the existing spec-drift block — details in context_summary.md
- Next: /harness-issue-prep the next P0 issue, then implement. Also refresh this repo's live .claude/hooks/*.sh from F003's fixed templates (still deferred)
- Blockers: none
- 2026-07-23 F002/OVI-46 merged @ 434308b; adversarial reviewer never reported after ~80 min despite a ping, so a lead self-review (with Ovidiu's explicit go-ahead, checked twice via AskUserQuestion) ran instead, found + fixed one real gap (assigned_to truthy check vs. spec's literal "!= null"), then merged on green CI; Linear OVI-46 Done

## Session 5 - 2026-07-23 (single-session)
- Feature: F004 - [OVI-49] P1.1 canonical feature schema (one owner) + stdlib validator
- Status: complete; PR #29 merged @ 5b15018; OVI-49 Done in Linear
- Tests: 145/145 passing (124 baseline + 21 new; TDD red phase confirmed all new fsv/z checks failing first)
- Coverage: n/a (shell suite, no coverage tooling; full_test 145/145 is the gate)
- Decisions: schemas/feature.schema.json (draft 2020-12) is the canonical envelope+16-field
  definition; scripts/validate-features.py hand-implements the same checks in stdlib Python
  (no jsonschema dep, per spec); only the 10 pre-v3.3 core fields are required, the 5 metric
  fields + spec are optional/type-checked-if-present for backward compat — details in
  context_summary.md
- Discovered: F022 (coverage field type: spec says number|null, this repo's own F001-F004
  store a descriptive string) — filed, not fixed, deliberately out of F004's scope
- Next: /harness-issue-prep the next P1 issue (this is P1.1; P1.2 and P2.2 both depend on
  this schema per the ticket's "Dependencies" note), then implement. Also refresh this
  repo's live .claude/hooks/*.sh from F003's fixed templates (still deferred); decide F022
- Blockers: none
- 2026-07-23 adversarial Opus review APPROVE with 3 non-blocking nits (all captured by
  F022 or cosmetic); CI green (2 checks); merged --merge on approval; Linear OVI-49 Done
- 2026-07-24 /harness-issue-prep OVI-50 -> F008: SV ASK (4 Qs answered), RV PASS (cycle 1); normalized spec + fresh spec.hash written to features.json; local-only (no stamp/label, Linear untouched)

## Session 6 - 2026-07-24 (single-session)
- Feature: F008 - [OVI-50] P1.2 one state boundary: shared harness_state.py for per-project hooks
- Status: implementation complete; PR pending, CI + adversarial review + merge in progress this session
- Tests: 170/170 passing (145 baseline + 25 new; TDD red phase confirmed 13 new hs: checks
  failing first, plus the single-writer grep check)
- Coverage: n/a (shell suite, no coverage tooling; full_test 170/170 is the gate)
- Decisions: harness_state.py.template writes only the .tmp file (final mv stays in the
  bash templates, preserving the grep-tested atomic pattern); increment-correction-cycles
  keeps the original id+status=='in-progress' match gate, exit 3 reserved for a truly
  missing id; session-start.sh delegates only the next-claimable algorithm, unchanged
  fallback for pre-v5 projects — details in context_summary.md
- Prep: /harness-issue-prep OVI-50 -> F008 this session: SV ASK (4 Qs, all answered),
  RV PASS cycle 1; normalized spec + fresh spec.hash written to features.json
- Next: /harness-issue-prep the next P2 issue (F009/OVI-51 or F010/OVI-52, both unblocked
  by P1.1). Also refresh this repo's live .claude/hooks/*.sh from F003's AND F008's fixed
  templates (still deferred); decide F022
- Blockers: none
- 2026-07-24 F008/OVI-50 merged @ d0af1ac; adversarial Opus review APPROVE with 3
  non-blocking nits (split-write footgun, no-fallback in check-remaining-tasks.sh,
  cosmetic stderr merge), no classifier block on this merge; Linear OVI-50 Done
- 2026-07-24 /harness-issue-prep OVI-51 -> F009: SV BLOCK (exit-code contradiction between the base spec and Amendment 5) -> 5 questions answered -> RV PASS cycle 1; remote mode (prep.linear/prep.stamp newly configured this session), Linear description updated + spec_hash written to features.json; UNSTAMPED (Keychain HMAC access blocked by the Bash tool sandbox, not an ACL issue; Ovidiu declined the sandbox override) -- no label applied

## Session 7 - 2026-07-24 (single-session)
- Feature: F009 - [OVI-51] P2.1 mechanize lead-only state ownership + best-effort Bash write boundary
- Status: implementation complete; PR pending, CI + adversarial review + merge in progress this session
- Tests: 205/205 passing (170 baseline + 30 new + 1 fixed pre-existing false positive;
  TDD red phase confirmed all new hs2: checks failing first)
- Coverage: n/a (shell suite, no coverage tooling; full_test 205/205 is the gate)
- Decisions: pre-existing out-of-scope Edit/Write check untouched (exit 2, legacy path);
  two new denial paths (lead-owned state files, Bash write boundary) use
  hookSpecificOutput.permissionDecision:"deny" (exit 0) with a verified-live annotation;
  heredoc bodies stripped before |/;/&& segmentation so payload text can't
  false-positive while heredoc-into-redirect lines still get caught — details in
  context_summary.md
- Scope expansion: added the other 3 gate templates (check-remaining-tasks,
  verify-git-identity, verify-task-quality) for the Failure posture: header,
  already implied by the RV-approved spec's own acceptance criteria
- Found and fixed: F008's single-writer grep test false-positived on legitimate
  json.dumps() calls; tightened to "json.dump(" 
- Next: implement F010/OVI-52 (P2.2) next in plan order. Also refresh live
  .claude/hooks/*.sh from F003's/F008's/F009's fixed templates (three things deferred
  now); decide F022
- Blockers: none
- 2026-07-24 adversarial Opus review (elevated risk) REQUEST CHANGES: found a real
  false-positive (redirect_target's first-match regex mistook a '>' inside a quoted
  string for the redirect operator, denying legitimate in-scope writes with
  markup/arrows/blockquotes); fixed with a one-line change (last match, not first) +
  regression test, 207/207; re-review APPROVE end-to-end against the real hook; merged
  @ 8ec5df5; Linear OVI-51 Done
- 2026-07-24 /harness-issue-prep OVI-52 -> F010: SV ASK (6 Qs) -> RV ASK (1 new gap: qa_binding field, cycle 1) -> RV PASS (cycle 2); remote mode, Linear description updated + spec_hash written to features.json; UNSTAMPED (same Keychain/Bash-sandbox block as OVI-51, confirmed persistent; Ovidiu declined the sandbox override again) -- no label applied

## Session 8 - 2026-07-24 (single-session)
- Feature: F010 - [OVI-52] P2.2 claim-matched proof, per-feature coverage target, delivery closure
- Status: implementation complete; PR pending, CI + adversarial review + merge in progress this session
- Tests: 247/247 passing (207 baseline + 40 new; TDD red phase confirmed 21 new
  fsv:/ht:/pf:/z:/w: checks failing first)
- Coverage: n/a (shell suite, no coverage tooling; full_test 247/247 is the gate)
- Decisions: 5 new optional feature fields (qa_binding, proof, coverage_target,
  delivered, design_contract), all backward-compatible; done-definition is now three
  tiers (passing -> done -> shipped); verify-task-quality.sh reads coverage_target and
  proof/qa_binding straight off the targeted feature at accept-time, no status check;
  session-end.sh's proof discipline note is informational only, not a
  SESSION_INCOMPLETE trigger — details in context_summary.md
- Found and fixed: two pre-existing test fixtures collided with new fields/behavior
  (F004's "unknown field" example used `proof`; session-end's "clean" fixture had
  F001 already passing with no proof) — both fixed, not worked around
- Next: /harness-issue-prep the next P2/P3 issue (F011-F021 remain). Also refresh live
  .claude/hooks/*.sh from F003's/F008's/F009's/F010's fixed templates (still deferred);
  decide F022
- Blockers: none
- 2026-07-24 adversarial Opus review APPROVE with 1 real nit (101-char line, fixed
  before merge) and 1 by-design observation; independently reconfirmed the F022 gap
  without being asked; merged @ e5f1fdf; Linear OVI-52 Done

## Session 9 - 2026-07-24 (single-session)
- Feature: F022 - coverage field type (discovered_via F004)
- Status: implementation complete; PR pending, CI + review in progress
- Tests: 250/250 passing (247 baseline + 3 new; TDD red phase confirmed 1 failing first)
- Coverage: n/a (shell suite, no coverage tooling; full_test 250/250 is the gate)
- Decisions: Ovidiu chose "relax the schema" (coverage: number|string|null) over
  rewriting the live data; live .harness/features.json now validates cleanly for
  the first time — details in context_summary.md
- Also: added a durable go-ahead default to global CLAUDE.md (skip the wait when a
  plan raises no open questions) and a risk/lane self-classification rule to this
  repo's CLAUDE.md (apply the existing dynamic-override heuristic without asking,
  per Ovidiu's request to reduce per-session confirmation overhead)
- Next: /harness-issue-prep the next P2/P3 epic issue (F005/OVI-61 next by priority).
  Also refresh live .claude/hooks/*.sh from F003's/F008's/F009's/F010's fixed
  templates (still deferred)
- Blockers: none
- 2026-07-24 F022 reviewed APPROVE with 1 non-blocking nit (scope array missing
  scripts/validate-features.py + test/run-tests.sh, fixed before merge); merged
  @ d0c8dff (no Linear issue, internal discovery via F004)
- 2026-07-24 /harness-issue-prep OVI-61 -> F005: SV ASK (1 Q: dogfood substrate contradiction) -> lead recommendation approved -> RV PASS (cycle 1, flagged combined approval for separate confirmation) -> Ovidiu rejected the soft middle ground, dropped the dogfood gate entirely (not deferred), scope reduced to hostile-gate tests only; reasoning posted as a Linear comment on OVI-61; remote mode, Linear description + spec_hash updated; UNSTAMPED (Keychain/Bash-sandbox block confirmed a third time, not re-asked)

## Session 9 (cont.) - 2026-07-24
- Feature: F005 - hostile-gate tests for the 4 mechanical hooks (OVI-61, scope
  narrowed; cold-start dogfood gate dropped entirely)
- Status: complete, merged
- Tests: 265/265 assertions passed (250 baseline + 15 new hostile-gate content
  assertions, 7 over the 8-minimum acceptance criterion)
- Coverage: n/a (shell suite, no coverage tooling; full_test 265/265 is the gate)
- Decisions: audited all 4 gates for existing content assertions first; 2 gates
  (enforce-scope's out-of-scope Edit, verify-git-identity's name-mismatch) already
  had them from earlier features, so only the gaps needed new assertions -- details
  in context_summary.md Meta-Session 2026-07-24 (session 9, F005/OVI-61)
- Next: /harness-issue-prep F006/OVI-62 (/harness-doctor instance health check +
  upgrade engine). Also refresh live .claude/hooks/*.sh from F003's/F008's/F009's/
  F010's fixed templates (still deferred, carried across many sessions now)
- Blockers: none
- 2026-07-24 F005 reviewed APPROVE (1 non-blocking wording nit on the PR
  description, no code/test change) by Opus reviewer who independently ran the
  suite, cross-checked all 15 new assertions against the actual .sh.template
  denial strings, and verified the email-mismatch test's isolation; merged @
  aa998df; Linear OVI-61 Done

- 2026-07-24 /harness-issue-prep OVI-62 -> F006: SV ASK (5 Qs, all undefined-term/consistency gaps) -> lead answered all 5 by grounding each in existing repo files (no invention) -> RV PASS cycle 1 (3/5 exact doc restatements, 2/5 evidence-backed lead policy calls; RV flagged the untracked-file default for Ovidiu's spot-check and the v3.6-list removal as a disclosed scope narrowing); remote mode, Linear description + spec_hash updated, local F006 spec object mirrored; UNSTAMPED (Keychain/Bash-sandbox block confirmed a fourth time, not re-asked)

## Session 10 - 2026-07-24
- Feature: F006 - /harness-doctor instance health check + upgrade engine (OVI-62)
- Status: complete, merged
- Tests: 307/307 assertions passed (288 baseline + 15 new in the first pass + 4
  more closing coverage gaps found via re-review)
- Coverage: ~98.6% (stdlib trace module; no coverage tool ships in this repo;
  full_test 307/307 is the gate)
- Decisions: scope expanded beyond the prep's SKILL.md-only scope to include
  doctor.py/fixes.py -- a testable "report-first structural check" needs an actual
  executable, matching the harness_state.py precedent; reused the plugin's own
  scripts/validate-features.py rather than duplicating validator logic (F004/OVI-49
  one-owner decision) -- details in context_summary.md Meta-Session 2026-07-24
  (session 10, F006/OVI-62)
- Next: /harness-issue-prep the next P2/P3 epic issue by priority. Also refresh
  live .claude/hooks/*.sh from F003's/F008's/F009's/F010's fixed templates (still
  deferred, carried across many sessions now)
- Blockers: none
- 2026-07-24 F006 first review: REQUEST CHANGES on a coverage-gate rejection alone
  (correctness never in question) -- reviewer measured ~79% combined via the
  stdlib trace module and named 5 specific untested behaviors by file:line. Closing
  them by hand (not just adding assertions to move a number) surfaced a real design
  bug: check_mld_non_injection was checking session-start.sh under the project's
  .claude/hooks/, but that file is never copied per-project (it runs directly from
  CLAUDE_PLUGIN_ROOT) -- the check was permanent dead code against any real
  project. Fixed to check the plugin's own copy. Re-review: APPROVE, independently
  re-measured coverage (full reachable-line coverage on the subset checked), all 5
  gaps confirmed closed with real fixtures/assertions, no scope creep (fixes.py
  byte-identical between commits, closed via new tests only). Merged @ c3767d2;
  Linear OVI-62 Done.

- 2026-07-24 /harness-issue-prep OVI-56 -> F007: SV ASK (2 Qs: plan_approval_response retest repro/criterion, run-#0 removal scope) -> lead answered both grounded in verified repo state -> RV PASS cycle 1 (RV caught and flagged one lead overreach -- an ungrounded CLI-version claim, not load-bearing); corrected stale line-number citations and a stale local ownership-note claim that F005 already created MAINTENANCE_LOG.md (it does not exist; F005 dropped that scope this session); remote mode, Linear description + spec_hash updated, local F007 mirrored; UNSTAMPED (Keychain/Bash-sandbox block confirmed a fifth time, not re-asked)

## Session 11 - 2026-07-25
- Feature: F007 - platform-drift maintenance loop (runbook + weekly CI cron +
  retirement-condition rule) (OVI-56)
- Status: complete, merged
- Tests: 326/326 assertions passed (307 baseline + 19 new maintenance-loop
  assertions across two review-response rounds)
- Coverage: n/a (shell suite, no coverage tooling; full_test 326/326 is the gate)
- Decisions: ran the plan_approval_response delivery-bug retest LIVE (spawned a
  real teammate) rather than reasoning from docs -- the round trip couldn't be
  completed at all (SendMessage has no plan_approval_request outgoing type;
  EnterPlanMode/ExitPlanMode aren't exposed to teammates), which is a bigger
  finding than a simple FIXED/BROKEN verdict and let us correct an actively
  wrong claim already in agent-teams-protocol.md -- details in
  context_summary.md Meta-Session 2026-07-25 (session 11, F007/OVI-56)
- Next: /harness-issue-prep the next P2/P3 epic issue by priority (F011/OVI-64
  commit-content gate is next claimable). Also refresh live .claude/hooks/*.sh
  from F003's/F008's/F009's/F010's fixed templates (still deferred, carried
  across many sessions now)
- Blockers: none
- Auth note: this session hit a git-push rejection ("refusing to allow an
  OAuth App to create or update workflow... without workflow scope") when
  pushing .github/workflows/maintenance.yml for the first time in this repo.
  gh auth refresh -s workflow plus /login (re-authenticating this session's
  own credential store, separate from a terminal-level gh token) were both
  needed before pushes worked from within this session -- worth remembering
  if a future session needs to touch .github/workflows/ for the first time.
- 2026-07-24/25 F007 first review: REQUEST CHANGES -- 3 blockers (a self-
  contradiction between a hedged correction and 3 unqualified instructional
  sites in the same doc; the correction overstating run #0's own evidence; a
  missing GH Actions permissions block that would have silently 403'd the
  exact failure-reporting path this feature exists for) plus test-quality
  gaps (spuriously-satisfiable assertions the reviewer proved by literally
  reverting the fix and confirming they still passed). Fixed and mutation-
  tested. Re-review: APPROVE with nits (a regression test that passed on both
  pre-fix and post-fix wording; a failure-title bug). Fixed those too,
  mutation-tested again, merged without a third full review round since the
  verdict was already APPROVE and the remaining fixes were narrow, verified,
  and directly tied to already-reviewed files. Merged @ 67d64a5; Linear
  OVI-56 Done.

- 2026-07-25 /harness-issue-prep OVI-64 -> F011: SV ASK (12 Qs, unusually many -- security-relevant secret-scanning gate) -> lead answered all 12 grounded in real repo files -> RV ASK cycle 1 (9/12 resolved; 2 explicitly routed to Ovidiu -- the no-echo security rule and the wiring-ownership fix -- plus a real content gap found: no git-diff-cached timeout) -> lead + Ovidiu resolved all 3 (Ovidiu re-delegated the 2 escalated items after being told why) -> RV ASK cycle 2 (accepted the informed re-delegation on the merits; found the coordination note needed to live in repo-tracked state, not just Linear) -> posted Linear comment on OVI-63 + mirrored into F013 local notes -> RV PASS cycle 3 (independently verified both); remote mode, Linear description + spec_hash updated, local F011 mirrored; lane=code, risk=ELEVATED (security-sensitive code); UNSTAMPED (Keychain/Bash-sandbox block confirmed a sixth time, not re-asked)

## Session 11 continued - 2026-07-25 (F011/OVI-64 shipped)
- Feature: F011 - Commit-content gate (secret scan + compound stage-and-commit denial)
- Status: complete, merged
- Tests: 480/480 passing (full_test); coverage 100% (185/185 executable lines
  in the embedded Python script, measured by tracing exactly what
  test/run-tests.sh's commit-gate.sh section exercises)
- Review: 6 adversarial rounds before APPROVE (details in context_summary.md's
  Meta-Session entry). Rounds 1-3 rewrote a regex-based git-subcommand
  detector into a real tokenizer after 3 rounds of regex patches each fixing
  one bug while introducing another. Rounds 4-6 did the same thing to the
  tokenizer's own supporting sets (GIT_FLAGS_WITH_VALUE, the continuation-join
  logic, the separator set) -- round 5 caught round 4's flag set was
  assembled from memory rather than probed; round 6 caught round 5's own
  continuation fix had introduced a new bypass, plus a missing "&" separator,
  plus a reserved-word recognition gap. Converged only once round 6 ran a
  96-case systematic fuzz sweep and found zero new bug classes. Every fix
  mutation-tested (reverted, confirmed the new test fails, restored) by me
  and independently re-verified by the round-5/6 reviewers themselves.
- Decisions: see context_summary.md Meta-Session 2026-07-25 for the full
  review history and lessons; features.json F011 approaches_tried has a
  detailed entry per round.
- Discovered: F023 (pending, priority 12) -- enforce-scope.sh.template has
  the same missing-newline-in-split-regex bug commit-gate.sh's round 3
  found and fixed, found by the F011 round-6 reviewer during idle time and
  verified against a real scope fixture; confirmed fail-open. Not fixed
  here (different, already-shipped hook, out of F011's scope).
- Merged: PR #40 @ 07ba2e4 (--merge, per repo convention; 8 commits preserved:
  implementation, 3 rounds of code+metadata fix pairs, F023 tracking, a
  version-precision correction).
  Linear OVI-64 Done. Local main synced, feature branch deleted (local +
  remote via gh api, since the git-push-based delete tripped the
  push-targets-main guard while HEAD was on main).
- Next: claim F023 or F012 ([OVI-53] portable readiness-stamp signing) to
  continue OVI-44. Also still deferred: refresh live .claude/hooks/*.sh from
  F003/F008/F009/F010's fixed templates.
- Blockers: none.

## Session 12 - 2026-07-26 (F023/enforce-scope.sh shipped)
- Feature: F023 - enforce-scope.sh.template missing-separator masking fix
- Status: complete, merged
- Tests: 530/530 passing (full_test); no coverage tooling for this shell suite
- Review: 3 adversarial rounds before APPROVE (details in context_summary.md's
  Meta-Session entry). Round 1's fix (add \n and & to segments_of's split)
  introduced a continuation-handling regression and a quoted-& false
  positive; both fixed by porting commit-gate.sh's join_continuations() and
  strip_quotes(). Round 2 found THAT fix (verbatim strip_quotes) was itself
  a critical regression: this hook's write target is routinely quoted
  (unlike commit-gate's, where quotes only wrap non-target text), so
  erasing quoted spans erased the target, disabling enforcement for the
  ordinary case of writing a quoted path -- worse than the bug F023 set
  out to fix. Fixed with a mask-then-slice redesign (mask_quotes finds
  separator positions in a placeholder-masked copy; segments are sliced
  from the ORIGINAL quoted text; a target is unquoted only after
  extraction via unquote_token). Round 3: independent reconfirmation of
  everything, one self-correction (the reviewer retracted their own
  "apostrophe-pairing hazard" finding after verifying it against real bash
  and finding it wasn't a real bug), APPROVE.
- Discovered: three more features filed during review, none fixed here:
  - F024 (priority 13): multi-target masking (rm/tee/sed -i with 2+ files;
    cp -t/mv -t destinations not recognized).
  - F025 (priority 11, ELEVATED severity): commit-gate.sh.template
    (already shipped, F011) has the SAME strip_quotes-erasure root cause
    as F023's round-2 finding -- quoting the "commit" token
    (`git "commit" -m x`) disables the ENTIRE gate including the secret
    scan. Verified end-to-end with a real staged secret. This is a live
    bypass of shipped security infrastructure, not a pending change --
    should be next priority.
  - F026 (priority 10): enforce-scope.sh's normalize() never resolves
    ".." path traversal; pre-existing on main before F023 (verified in
    real bash), F023's fix incidentally exposed 2 more spellings of it.
- Merged: PR #42 @ 1e8e30f. Not tied to a Linear issue (locally discovered
  bug, matching F022's precedent). Local main synced, feature branch
  deleted (local + remote via gh api).
- Next: F025 first (live security bypass in shipped infra, highest
  severity of the three new filings), then F026, F024, or F012
  ([OVI-53] portable readiness-stamp signing). Also still deferred:
  refresh live .claude/hooks/*.sh from F003/F008/F009/F010's fixed
  templates.
- Blockers: none.

## Session 13 - 2026-07-27 (F025/commit-gate.sh shipped)
- Feature: F025 - commit-gate.sh.template quoted-subcommand-token bypass fix
- Status: complete, merged
- Tests: 566/566 passing (full_test); no coverage tooling for this shell suite
- Review: 2 adversarial rounds before APPROVE (details in context_summary.md's
  Meta-Session entry). Round 1 found the fix's mask-then-slice design was
  correct at the segment level but left tokenization inside a segment naive
  (seg.split()), which the OLD quote-deletion design had accidentally masked
  -- exposed a fail-open (a quoted "--" anywhere in a commit message could
  shadow a real trailing staging flag) and a false positive (flag-shaped
  words in a quoted message). Fixed with split_tokens(), the same mask-then-
  slice discipline one level deeper. Round 2: independent reconfirmation,
  no new code findings, one documentation error caught and self-corrected
  by the reviewer (a ticket's polarity was backwards on first filing,
  verified and fixed against real git before I acted on it).
- Discovered: two more features filed during review, none fixed here:
  - F027 (priority 11): commit-gate.sh's has_staging_flag() doesn't
    account for a space-separated -m's value as opposed to an attached
    one, surfacing as 3 shapes on the same root cause -- 1 pre-existing
    false-negative, 1 false-negative F025 narrowed, 1 false-positive F025
    introduced. One fix should close all three.
  - F028 (priority 14, low severity): the analogous quoted-path-truncation
    bug exists in enforce-scope.sh.template too, but isn't exploitable
    today (no space-containing entries in the one exact-match set that
    would be affected).
- Merged: PR #44 @ 647eeb8. Not tied to a Linear issue (locally discovered
  bug). Local main synced, feature branch deleted (local + remote via gh
  api, since git-push-based delete trips the push-targets-main guard).
- Next: F024, F026, F027, or F028 (all well-scoped, none urgent), or F012
  ([OVI-53] portable readiness-stamp signing). Also still deferred: refresh
  live .claude/hooks/*.sh from F003/F008/F009/F010's fixed templates.
- Blockers: none.

## Session 14 - 2026-07-28 (F024/enforce-scope.sh shipped)
- Feature: F024 - enforce-scope.sh.template multi-target masking fix
- Status: complete, merged
- Tests: 627/627 passing (full_test); no coverage tooling for this shell suite
- Review: 4 adversarial rounds before APPROVE (details in context_summary.md's
  Meta-Session entry) -- the longest single-PR cycle since F011's original 6,
  but the first one in this chain fully self-contained within one PR. Round 1
  found the sed script-detection heuristic broke on 3 real shapes (BSD
  `-i ''`, multi `-e`, long-form `--expression=`/`--file=`) plus a cross-
  extractor masking gap. Round 2 found a stray file-descriptor digit left
  behind by the redirect-stripping fix, misread as a bogus target. Round 3
  found the round-2 fix over-anchored (broke plain `>` with no digit glued
  to an argument with no space). Round 4: no new code regression, only a
  test-quality nit (fixed and mutation-verified), APPROVE.
- Discovered: two more features filed during review, plus one finding
  folded into an existing ticket:
  - F029 (priority 15): enforce-scope.sh doesn't recognize "--" as a
    pathspec separator the way commit-gate.sh does.
  - F030 (priority 16): enforce-scope.sh denies `2>/dev/null` and `2>&1`
    (both common idioms) as out-of-scope writes; confirmed pre-existing,
    unrelated to F024/F025.
- Merged: PR #46 @ ce8dcc8. Local main synced, feature branch deleted.
- Next (per "take them in order"): F026, then F027, F028, F029, F030.
- Blockers: none.

## Sessions 15-18 - 2026-07-28/29/30 (F026, F027, F028, F029, F031, F030, F033, F034, F032 shipped)
- Worked the local-bug backlog in roughly priority order, each through the
  standard loop (TDD, mutation-tested fixes, adversarial review to APPROVE,
  merge --merge, features.json update). All nine features now `passing`:
  - F026 (PR #48): enforce-scope.sh normalize() now resolves ".." path
    traversal instead of leaving it unresolved after root-prefix stripping.
  - F027 (PR #49): commit-gate.sh has_staging_flag() now skips a
    space-separated value-taking short flag's OWN value token instead of
    misreading it as `--` or a flag; closed 3 related shapes at once.
  - F028 (PR #51): enforce-scope.sh redirect_target() no longer truncates a
    quoted path at its first internal space.
  - F029 (PR #52, 4 rounds): enforce-scope.sh's all_flagless_tokens() now
    recognizes "--" as a pathspec separator like commit-gate.sh does.
  - F031 (PR #50): enforce-scope.sh unquote_token() now decodes shell
    backslash-escapes, closing a path-traversal-via-escaped-slash gap.
  - F030 (PR #53, round 4 merge): enforce-scope.sh no longer denies the
    `2>/dev/null` / `2>&1` idioms as out-of-scope writes.
  - F033 (PR #57): enforce-scope.sh unquote_token() now decodes bash's real
    ANSI-C ($'...') backslash-escape grammar instead of leaving it raw.
  - F034 (PR #58, round-4 notes-only follow-up in PR #60): commit-gate.sh's
    command_segments() no longer splits on the "&" inside `2>&1`/`&>`/`&>>`
    fd-duplication idioms, closing a real compound-stage-and-commit bypass
    (not just a usability false-positive, unlike enforce-scope's F030).
  - F032 (PR #56, 2 rounds): commit-gate.sh has_staging_flag() now
    recognizes space-separated long-flag VALUES (--message, --file,
    --trailer, etc. -- grew from 8 to 14 value-taking long flags) and
    resolves abbreviated long flags via git's own unambiguous-prefix rule
    (_resolve_long_flag()), closing both a live `--trailer`-shaped bypass
    and an abbreviation bypass (`--mess`, `--trail`, etc. all genuinely
    stage+commit in real git).
  - Recurring pattern across this whole arc: fixing one hook's bug
    routinely surfaced the same bug class in a sibling hook, or a narrower
    residual in the same function -- each review round's own findings fed
    the next feature's filing (F035-F040, see below).
- Tests: 823/823 passing on main as of this entry (full_test is the gate;
  no coverage tool for this shell suite).
- Discovered, still open (all internally discovered, no Linear issue,
  status "pending" in features.json):
  - F035 (priority 19, discovered_via F028): enforce-scope.sh's
    cp_mv_targets()/sed_inplace_targets() don't recognize a QUOTED "--" or
    quoted flag the way an unquoted one is recognized. Fix implemented and
    pushed as PR #59 (2 rounds so far: round 1 fixed 2 of 4 "--"-recognition
    call sites, round 2 closed the other 2 plus 8 mutation-coverage gaps a
    reviewer found, plus filed F040 below). Round-2 review verdict not yet
    received as of this entry -- reviewer agent pinged, awaiting response.
  - F036 (priority 20, discovered_via F030): enforce-scope.sh's
    redirect_targets()/strip_redirects() gap on `>&word`-shaped redirects.
  - F037 (priority 21, discovered_via F029): sed_inplace_targets() doesn't
    recognize GNU/BSD `-i`'s clustered or long-form (`--in-place=`) spellings.
  - F038 (priority 22, discovered_via F033): ANSI_C_ESCAPE_PATTERN doesn't
    decode `\c`/`\u`/`\U` escapes (confirmed non-exploitable: produces
    non-printable bytes only, never `.`/`/`).
  - F039 (priority 23, discovered_via F033): enforce-scope.sh's
    target-extraction pipeline doesn't truncate on an embedded NUL the way
    real argv parsing would.
  - F040 (discovered_via F035, filed on the F035 branch, not yet in main's
    features.json): write_targets() compares command_tokens[0] raw against
    the cp/mv/tee/rm/sed name sets, so `\rm`, `"rm"`, `'rm'` all evade
    recognition entirely.
- Blocked, unchanged since session 11: F012/OVI-53 and F013/OVI-63 both
  await Ovidiu's answers to open spec-verification questions from an
  earlier session; no Linear-tracked work possible until he responds.
  F014-F021 not yet started.
- Next: resolve PR #59 (F035) once its round-2 verdict lands, then pick up
  F036-F040 in the same loop. Still deferred: refresh live .claude/hooks/*.sh
  from F003's/F008's/F009's/F010's fixed templates; draft CHANGELOG catch-up
  once a version bump is confirmed with Ovidiu.
- Blockers: F012/F013 only (see above); no blockers on the local-bug loop.

## Sessions 19-25 - 2026-07-29 to 2026-07-31 (F035-F062 shipped; local-discovery chain closed)
- F035-F054 (20 features, no Linear issue): all shipped in earlier windows
  of this same multi-session arc, each through the standard TDD +
  mutation-test + adversarial-review-to-APPROVE loop. Full detail is in
  each feature's own features.json `notes` field (not duplicated here) --
  highlights: F035/F040-F048 closed the remaining enforce-scope.sh/
  commit-gate.sh quoting, escaping, and abbreviation-resolution gaps
  (mirroring real git/GNU-getopt_long behavior); F049-F054 covered a
  sed backup-suffix write-target gap, a raw command-name comparison
  bypass, and the F054 secret-scan-exemption mechanism (which itself
  needed a round-3 correction after a false "harness.json is lead-owned"
  claim was caught and corrected -- that correction is what filed F058).
- F055-F062 (this session, 2026-07-31): the LEAD_OWNED protection family
  plus TeammateIdle/shutdown coordination fixes. F055: role-aware
  TeammateIdle guidance (agents/reviewer.md dedup instruction + hook
  stderr wording), 2 review rounds. F056: cp_mv_targets() argv-permutation
  destination bypass (--suffix/-S/--no-preserve/--sparse value-consuming
  flags), 4 rounds -- the deepest single-feature cycle of the session,
  see Meta-Session 2026-07-31 in context_summary.md for why. F057:
  verify-task-quality.sh's exit-0 accept-path warnings were genuinely
  invisible to teammates (confirmed via direct docs fetch), fixed via the
  documented `systemMessage` JSON field, 2 rounds. F058: .harness/
  harness.json made genuinely LEAD_OWNED, 3 rounds (stale commit-gate.sh
  comments caught twice, plus a comment reflow claim that hadn't actually
  landed). F059: per-teammate early release for role-limited teammates --
  investigation redirected this from a code fix to a documented lead
  judgment rule in rules/agent-teams-protocol.md, 2 rounds. F060:
  .claude/teammate-scope.txt made genuinely LEAD_OWNED, 3 rounds (a false
  "lead is unaffected" claim, then an overclaimed-completeness comment).
  F061: investigated whether a hook can distinguish the lead's session
  from a teammate's -- confirmed via direct docs fetch that no such
  mechanism exists; resolved as a documented "Known limitation" rather
  than a workaround, confirmed with Ovidiu before shipping the redirected
  scope; 4 rounds, the most self-referential chain of the session (each
  round's own restated justification became the next round's finding).
  F062: .harness/mld/ made genuinely LEAD_OWNED via the first prefix-style
  LEAD_OWNED entry (mld files are dated/session-named, not a fixed path),
  2 rounds -- round 1 found the actual new mechanism had zero
  discriminating test coverage, reproduced as a live false positive
  before fixing. This closes the entire local-discovery chain that
  started with F023.
- Tests: 1342/1342 passing on main as of this entry (full_test is the
  gate; no coverage tool for this shell suite).
- Blocked, unchanged since session 11: F012/OVI-53 and F013/OVI-63 both
  await Ovidiu's answers to open spec-verification questions from an
  earlier session; no Linear-tracked work possible until he responds.
- Next: F015-F021 (the seven remaining Linear OVI-44 sub-issues) were
  deliberately deferred until the local-bug chain finished. It has --
  next session should either get F012/F013's answers from Ovidiu or start
  F015 via `harness-issue-prep`. No other locally-discovered work queued.
- Blockers: F012/F013 only (see above); no blockers on Linear re-engagement.

## Session 26 - 2026-07-31 (F012/OVI-53: portable readiness-stamp signing)
- Feature: F012 - portable readiness-stamp signing (Linux/CI parity for the spec gate)
- Status: complete
- Corrected a stale claim first: the "F012/F013 blocked awaiting Ovidiu's answers"
  claim repeated since session 11 was ground-truthed and found stale (spec.verdict
  already PASS for both, no hash drift, zero open Linear comment threads). Reported
  this to Ovidiu; he confirmed to proceed ("go").
- Implemented per the full Linear spec (OVI-53, re-fetched via get_issue -- the local
  features.json description is a terse summary, not the authoritative text): a
  3-source secret resolution chain (Keychain, then VV_HARNESS_STAMP_KEY_FILE at mode
  0600, then discouraged VV_HARNESS_STAMP_KEY env var), python3-stdlib HMAC
  computation, and a flat, presence-gated prep.kick_command replacing the hardcoded
  launchctl-only Step 8.
- Caught and corrected one real spec-conformance bug before commit: the first
  implementation pass nested the new config key as prep.runner.kick_command with a
  leftover enabled flag, only caught by re-checking the actual Linear issue text
  against the local terse summary before finalizing.
- Tests: 1355/1355 passing on main (full_test is the gate; +13 new F012 assertions,
  test-by-extraction from the real shipped SKILL.md snippet, not a parallel
  re-implementation). Mutation-tested by reverting the 0600 permission check and
  confirming exactly the 2 related assertions failed; restored.
- Scope expansion: INSTALL.md's kickstart_label example updated to match the
  generalized prep.kick_command (not in the original scope list, but would have
  gone stale otherwise).
- Next: F013/OVI-63 (Ovidiu's "go" covered both F012 and F013). F015-F021 remain
  deferred until F013 lands.
- Blockers: none.

## Session 27 - 2026-08-01 (F013/OVI-63: mechanical stamp for /harness-init)
- Feature: F013 - mechanical stamp for /harness-init (+ upgrade mode)
- Status: complete
- Implemented per the full Linear spec (OVI-63, fetched via get_issue): scripts/stamp.sh
  emits every framework-fixed file (settings.json rendered from a template, 7 hooks
  copied byte-verbatim then chmod +x, harness.json/features.json skeletons) from a
  KEY=VALUE answers file. mode=new aborts on any collision, writing nothing; mode=upgrade
  refreshes only byte-identical-to-a-fresh-stamp files and skips+reports anything that
  differs, never silently overwriting a customization.
- harness-init/SKILL.md Steps 3/3.5/3.6 shrink to calling the stamp (AC1: zero remaining
  inline "hooks" JSON). Caught and fixed a real step-ordering bug: a build-hook
  confirmation step was drafted to run physically after the step it needed to precede --
  folded into the start of Step 3 instead of renumbering every subsequent step (which
  would have had a real blast radius, since INSTALL.md and harness-doctor's fixes.py both
  point at "Step 3.6" by number).
- Tests: 1385/1385 passing on main-bound branch (full_test is the gate; +19 assertions
  incl. a pre-existing test redirected to the new template file it now checks).
  Mutation-tested 3 behaviors; the first mutation round exposed 2 weakly-discriminating
  assertions (fixed with an explicit "says aborted" check and an mtime-unchanged check).
  Directly verified AC3 (a stamped project passes harness-doctor clean) by hand before
  encoding it as a test.
- Considered and declined: wiring harness-doctor's --fix to delegate to stamp.sh (a
  Dependencies-section consequence noted in the spec, not an acceptance criterion; out
  of this feature's declared scope).
- Next: F015-F021 (the seven remaining Linear OVI-44 sub-issues), per Ovidiu's standing
  instruction to keep working through everything from Linear without waiting for
  check-ins.
- Blockers: none.

## Session 28 - 2026-08-01 (F015/OVI-55: promotion ladder + ablation pass)
- Feature: F015 - promotion ladder + ablation pass in the Phase 5.5 retrospective
- Status: complete
- Documentation/skill-content only, no code. Added two mandatory passes to
  harness-continue's Phase 5.5: a Promotion pass classifying each Meta-Pattern/
  corroborated-MLD entry to its smallest durable owner (7-rung ladder:
  spawn-prompt tweak -> rule file edit -> hook change -> schema field -> agent
  definition -> plugin skill -> not-yet), and an Ablation pass giving every
  control that fired this session a retain/revise/remove verdict.
- New .harness/HARNESS_BACKLOG.md schema (date/observation/proposed
  owner/evidence pointer/score/status/last_seen). Promotion requires score>=3
  from distinct sessions; candidates decay to retired after 60 days stale; a
  hard-capped 15-line "Learned conventions" section is the only permitted
  always-on promotion target; gap entries get their own row type with a 3+
  promotion threshold. rules/context-summary.md documents the new Meta-Patterns
  disposition marker (promoted-to/backlog/watching) with a worked example.
- P3.1 (corroborated MLD entries) hasn't shipped in this repo -- documented as
  optional input, skipped gracefully when absent.
- Self-caught a cross-reference bug before finishing: a "rule below" pointer
  was wrong (the rule is above, in Step 5a) -- same read-it-back discipline
  that caught F013's step-ordering bug.
- Tests: 1400/1400 passing (full_test is the gate; +11 F015 assertions incl.
  an AC7 "stated exactly once" guard, mutation-tested).
- AC3's dry-run transcript is documented in the PR description (a classified
  pattern + an ablation verdict), not committed as a file -- HARNESS_BACKLOG.md
  is a per-project runtime artifact, not plugin distribution content.
- Next: F016-F021 (six remaining Linear OVI-44 sub-issues) and F063
  (discovered_via F013), per Ovidiu's standing instruction to keep working
  through everything from Linear without waiting for check-ins.
- Blockers: none.

## Session 29 - 2026-08-01 (F016/OVI-57: worker epoch record + requalification checklist)
- Feature: F016 - worker epoch record + requalification checklist (with subtraction pass)
- Status: complete
- Added an optional harness_worker_block sub-schema to schemas/feature.schema.json
  (cli_version, models{lead,implementer,reviewer}, recorded_at) for harness.json's
  optional worker block -- data, not policy (policy stays in agent frontmatter and the
  protocol bindings table).
- harness-continue gains Step 2.6 (Worker Epoch Check): runs `claude --version`
  defensively, compares the minor-version delta against worker.cli_version, prompts
  requalification at delta>=10 (or a reported model-generation change), skips silently
  when the CLI probe fails or the worker block is absent.
- New docs/requalification.md checklist: orientation eval re-run, bindings-table
  review, operational-metric epoch-scoping note, inherit-vs-pinned per-role decision
  (reviewer is the first inherit candidate), verified-live annotation convention,
  degradation-is-not-escalation guidance, and a 4-candidate subtraction pass table.
- rules/agent-teams-protocol.md's existing Model Selection table reframed as the
  single bindings table (requalification updates a row, never the protocol prose)
  plus a metrics-hygiene sentence scoping historical signals to the current epoch.
  templates/CLAUDE.md gains the CLAUDE_CODE_SUBAGENT_MODEL footgun warning.
- Tests: 1410/1410 passing (full_test is the gate; +9 F016 assertions, 3
  mutation-tested).
- Next: F017-F021 (five remaining Linear OVI-44 sub-issues) and F063
  (discovered_via F013), per Ovidiu's standing instruction to keep working through
  everything from Linear without waiting for check-ins.
- Blockers: none.
