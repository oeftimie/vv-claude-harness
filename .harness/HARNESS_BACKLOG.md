# Harness Backlog

Candidates the Phase 5.5 promotion and ablation passes have surfaced. Not
auto-applied: a human or a dedicated session executes a row, then marks it
promoted. Cross-project pattern aggregation is a future extension, not done
here -- this file is per-project.

| date | observation | proposed owner | evidence pointer | score | status | last_seen |
|---|---|---|---|---|---|---|
| 2026-08-09 | Recurring bug classes get fixed one call site at a time (ARG_MAX argv class needed 3 fixes across 3 sessions); no rule tells the implementer to sweep the class | rule file edit (tdd.md or debugging.md: "when fixing an instance of a recurring class, grep for the class") | Meta-Session 2026-08-09 (batch), F096 + review round | 1 | candidate | 2026-08-09 |
| 2026-08-09 | Feature scope arrays omit test/run-tests.sh even though every feature writes tests there; enforce-scope would deny a teammate its own TDD writes | skill (harness-issue-prep/harness-init: default the project's test-runner path into scope for testable features) | review finding "F094/F095/F096 edited test/run-tests.sh outside their recorded scope" | 1 | candidate | 2026-08-09 |
| 2026-08-09 | Multi-dimension review workflows verify duplicate findings separately (~2x verify cost); dedup barrier before verify fan-out is the fix | not-yet (workflow authoring pattern, no durable owner file yet) | Meta-Session 2026-08-09 (batch), review workflow wf_62aae2ac-e3b | 1 | candidate | 2026-08-09 |
| 2026-08-09 | Ablation: commit-gate denied a non-staging `git commit --no-edit 2>&1` one-liner. CORRECTED diagnosis (first row version blamed && chains -- wrong): the F052 bare-pathspec rule read the unquoted redirection `2>&1` as a pathspec; chains/pipes segment correctly. Fixed as F109 (redirections skipped when the RAW token is unquoted/unescaped; quoted `'2>&1'` still denies as a real pathspec) | hook change | F109, v5.6.1 | 1 | promoted | 2026-08-09 |
| 2026-08-09 | Push guard blocked `git push origin v5.6.0` (a tag push) while checked out on main. Two causes found: the "main" substring scan ran over the WHOLE compound command (a `git log origin/main` in a later segment triggered it), and the bare-push branch check ignored explicit non-main refspecs. Fixed in ~/.claude/hooks/git-guard.sh (personal hook, not repo content): per-segment evaluation, refspec-aware (HEAD counts as current branch; --all/--mirror/--branches treated as branch-carrying; bare push keeps the branch check; unresolvable still asks). 20-case matrix verified under /bin/bash 3.2 | hook change (personal, done) | v5.6.0 release phase blocks; fix session 2026-08-09 | 1 | promoted | 2026-08-09 |
| 2026-08-12 | An AC that deletes/moves a file touched by a sibling in-flight phase can be verified-true at prep yet stale at execution (AC14 vs F113's team-spawn-prompts.md repurposing); nothing forces a ground-truth read before executing destructive ACs | skill (harness-continue: before executing a delete/move AC, read the target and reconcile against ACs verified earlier) | Meta-Session 2026-08-12, AC14 deviation | 1 | candidate | 2026-08-12 |
| 2026-08-12 | Spec-gate normalization writes grep-based ACs whose allow-lists omit the test suite's own absence assertions and historical-record files; the asymmetry (AC21 vs AC22) surfaced only at execution | skill (harness-issue-prep Step 5: when an AC is a repo-wide grep, default the allow-list to include test-absence-assertions and append-only records) | Meta-Session 2026-08-12, AC21/AC22 amendments | 2 | candidate | 2026-08-12 |
| 2026-08-12 | Parallel-agent deaths at the account session limit are recoverable when work lives in worktrees and prompts mandate file-based summaries; salvage = lead commits the partial diff, respawns a scoped continuation agent (recurred 2026-08-14: two F117 drill runs killed mid-flight; recovered via state re-read + relaunch after reset) | not-yet (worked twice as improvised procedure; watch for recurrence before promoting into parallel-work.md) | Meta-Session 2026-08-12, wp33 salvage | 2 | candidate | 2026-08-14 |
| 2026-08-13 | Spec-verification grounds file/line claims by reading files, but claims about TEST-SUITE state (e.g. 'drop the dedicated ts assertions' when none exist; baseline assertion counts) go unverified -- prep should grep the suite for asserted-artifact claims the same way it greps source | rule file edit | Meta-Session 2026-08-13 (F116) | 1 | candidate | 2026-08-13 |
| 2026-08-14 | CORRECTED diagnosis (first version called this a missing script feature -- wrong): rules/parallel-work.md says twice to "upgrade its implementer to Opus" for elevated features (lines 145, 169) while the SAME section and OVI-140's decided model policy both scope escalation to the REVIEW stage only, which is exactly what implement-features.js implements. The script is correct; the rule carries Teams-era residue Phase 4 did not sweep -- the drift the epic itself predicted ("two mechanisms describing the same policy will drift"). Separately genuine: the rule's historical-signals table tells the lead to upgrade an implementer after correction_cycles >= 3, and workflow mode gives the lead no lever to do it -- PROMOTED 2026-08-15: shipped as F118 in v6.0.1 (rule sweep of the Teams-era implementer-upgrade residue, plus an optional per-feature implementModel lever); the row was never flipped when the work landed | rule file edit (sweep the implementer-upgrade residue) + workflow script (optional per-feature implementModel, mirroring reviewModel, default unchanged) -- resolution is Ovidiu's spend call | eval doc AC1 findings, F117; rules/parallel-work.md:145,165,169 vs workflows/implement-features.js:165-189 | 1 | promoted | 2026-08-15 |
| 2026-08-14 | Following harness-continue Step 3.5 (conformance proof on elevated features) guarantees a qa_binding/proof.evidence_type mismatch warning from verify-task-quality.sh -- two shipped rules contradict -- PROMOTED 2026-08-15: shipped as F119 in v6.0.1 (verify-task-quality exempts conformance proof on elevated features); the row was never flipped when the work landed | hook change (verify-task-quality: accept conformance as satisfying a unit qa_binding) | eval doc AC1 findings, F117 | 1 | promoted | 2026-08-15 |
| 2026-08-14 | TaskCompleted coverage stage self-skips silently when the stack has no coverage tool; two toy leads independently flagged coverage:null as unverifiable -- PROMOTED 2026-08-15: shipped as F120 in v6.0.1 (a declared-but-unmeasured coverage target now emits a visible non-blocking note); the row was never flipped when the work landed | skill (harness-init: ask about coverage tooling at init; init.sh template reports the skip loudly) | eval doc AC1 + AC3 findings (two sessions), F117 | 2 | promoted | 2026-08-15 |
| 2026-08-14 | Recovery from an interrupted run verified code but had to manually spot an unauthorized out-of-scope settings edit in the surviving worktree (the Workflow deny removed) -- PROMOTED 2026-08-15: shipped as F121 in v6.0.1 (worktree scope-diff before merging a branch from an unwatched run); the row was never flipped when the work landed | skill (harness-continue recovery: diff every worktree-touched file against the feature's declared scope before merging) | eval doc AC2d/AC3 integrity catch, F117 | 1 | promoted | 2026-08-15 |
| 2026-08-14 | harness-init's init.sh template python branch pipes tests through `2>&1` into `tail`, masking failures under set -e without pipefail; the init agent detected and avoided it locally | rule file edit / template fix (skills/harness-init/init.sh.template) | eval doc fresh-init record, F117 | 1 | candidate | 2026-08-14 |
| 2026-08-14 | release-consistency's tag assertion runs on push-to-main, which is ALWAYS before the release tag push, so every release auto-opens an identical false-positive drift issue (#157 v5.5.0, #159 v5.6.0, #161 v5.6.1, #164 v5.7.0, #169 v6.0.0 -- 5 consecutive) and nothing closes them -- PROMOTED 2026-08-14: tag assertion gated off push-to-main (GITHUB_REF_TYPE), carried by tag-push + daily schedule; workflow now closes a cleared drift issue itself; 9 stale issues closed by hand | hook change (.github/workflows/release-consistency.yml: gate the tag assertion on the tag-push event, or close the prior issue when the gap clears) | gh issue list 2026-08-14; issue #169 closed by hand after verifying the tag | 5 | promoted | 2026-08-14 |
| 2026-08-14 | Two doctor health assertions compared raw output to the literal "healthy", so the workflow-support notices OVI-144 shipped (INFO/WARN, documented as never changing the exit code) failed the suite in any environment without a `claude` CLI -- main was red in CI from 2026-08-12 and two releases (v5.7.0 wasn't affected; OVI-146 and OVI-147) merged over it unnoticed | hook change / process (nobody checks Actions before merging a PR; consider requiring the tests check on the branch protection rule) | CI runs 31732592193 (main), 31777114813 + 31777093411 (ovi-147 branch); fixed on eovidiu/ci-green-doctor-notice-filter -- PROMOTED 2026-08-15 (OVI-150): prep established the required context is `test`, not the `tests / test` the issue assumed (nothing publishes that string; requiring it would have blocked every merge), and that only `strict` prevents the stale-base class this row's sibling #175/#176 case showed. Applied 2026-08-15 as a repository RULESET, not a classic rule: two duplicate rulesets already existed (created 2026-08-14, strict off); Ovidiu deleted the duplicate and enabled strict. The classic `branches/main/protection` endpoint 404s while the ruleset enforces, which is exactly why this row's issue was written believing main had no protection at all -- recorded in AGENTS.md so the next reader does not repeat it | 1 | promoted | 2026-08-15 |
