## Mistakes
- Wrote the F114 features.json entry without the schema-required `notes`/`approaches_tried`/`discovered_via`/`linear` fields; the suite's own validator caught it (wp31-32 reported it as a pre-existing failure).
- Proposed AC3's orphan-grep command in the human loop without running it against the repo first; the reverification guard ran it and found 16 unlisted files. Two more miscounts (CLAUDE.md hits, schema pointer count) in my cycle-2 dispositions, also guard-caught.
- Drafted AC21 without the test-absence-assertions allowance that AC22 had; surfaced as an execution-time amendment instead of a prep-time fix.

## Learnings
- Ground-truth destructive ACs at execution time: AC14's deletion target had been repurposed by the sibling phase after the spec was written but before it was executed.
- A dead worktree agent is a checkpoint, not a loss: commit its diff, respawn a continuation agent scoped to what's missing (wp33 hooks survived, tests respawned).
- Repo-wide greps inside the test suite must prune .claude/worktrees/ or live parallel agents corrupt the counts.

## Desires
- A spec-hash drift check at implementation start (re-fetch the issue, compare against the stamp) -- the stamp records it but nothing verifies it before work begins.
- A doctor probe (or CI job) that runs the AC-grep style orphan checks continuously instead of once at phase close.
- Subagent-level visibility into remaining account budget so the lead can sequence risky long agents before the limit window.

## F115 addendum (same session)
- Mistake: proposed the AC6 exemption list without running the grep -- the guard found 5 uncovered files; same class as the F114 AC3 miss. The backlog row is at score 2; promote at 3.
- Learning: sending the RV guard a delta prompt with a paraphrase of an unchanged AC wastes a cycle -- it correctly refuses to trust paraphrases; always resend full spec text.
- Learning: doctor.py had 5 same-class "retired in v5.7.0" strings where the spec cited 1 comment; agents that escalate out-of-scope same-class findings to the lead (rather than fixing or ignoring) are behaving exactly right.
